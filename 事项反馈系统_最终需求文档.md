# 事项反馈系统（最终合并版需求文档）

> 版本：v1.0（可直接交付开发）  
> 目标：一个**内部使用、轻量、可扩展**的事项收集 / 反馈 / 跟踪工具，支持后续文件、导出和提醒能力。

---

## 一、总体目标与设计原则

### 1. 建设目标
- 用于**发起事项 → 多人反馈 → 汇总与跟踪**
- 替代微信群 / Excel / 邮件反复统计
- 支持最小可用（MVP），后续平滑扩展

### 2. 设计原则
- 前后端分离
- API 简单、可 Mock
- 先流程正确，再优化体验
- 权限最简（不做复杂 RBAC）

---

## 二、核心业务对象（数据模型）

### 1. 用户（User）
```json
{
  "id": "uuid",
  "name": "string",
  "email": "string",
  "role": "admin | normal",
  "created_at": "datetime"
}
```

---

### 2. 事项（Item）
```json
{
  "id": "uuid",
  "title": "string",
  "description": "string",
  "creator_id": "uuid",
  "status": "draft | collecting | closed",
  "deadline": "date",
  "created_at": "datetime"
}
```

---

### 3. 反馈（Feedback）
```json
{
  "id": "uuid",
  "item_id": "uuid",
  "user_id": "uuid",
  "content": "string",
  "status": "submitted | withdrawn",
  "created_at": "datetime"
}
```

---

### 4. 附件（Attachment，v2 启用）
```json
{
  "id": "uuid",
  "related_type": "item | feedback",
  "related_id": "uuid",
  "filename": "string",
  "filepath": "string",
  "uploaded_at": "datetime"
}
```

---

## 三、后端 API 设计（Skeleton + 可直接实现）

### 1. 用户接口

| 方法 | 路径 | 描述 |
|---|---|---|
| GET | /api/users | 用户列表 |
| POST | /api/users | 新建用户 |
| GET | /api/users/{id} | 用户详情 |

---

### 2. 事项接口

| 方法 | 路径 | 描述 |
|---|---|---|
| GET | /api/items | 获取事项列表 |
| POST | /api/items | 创建事项 |
| GET | /api/items/{id} | 事项详情 |
| PUT | /api/items/{id} | 更新事项 |
| DELETE | /api/items/{id} | 删除事项 |

---

### 3. 反馈接口

| 方法 | 路径 | 描述 |
|---|---|---|
| GET | /api/items/{id}/feedbacks | 事项反馈列表 |
| POST | /api/items/{id}/feedbacks | 提交反馈 |
| PUT | /api/feedbacks/{id} | 修改/撤销反馈 |

---

### 4. 文件接口（v2）

| 方法 | 路径 | 描述 |
|---|---|---|
| POST | /api/upload | 上传文件 |
| GET | /api/export/item/{id} | 导出事项数据 |

---

## 四、前端页面清单

### 1. 页面列表

| 页面 | 路径 | 描述 |
|---|---|---|
| 登录页 | /login | 简单身份进入（可选） |
| 事项列表 | /items | 所有事项 |
| 新建事项 | /items/create | 创建事项 |
| 事项详情 | /items/:id | 填反馈 / 看汇总 |
| 我的反馈 | /my-feedback | 当前用户已提交内容 |

---

## 五、前端路由表（Vue / React 通用）

```ts
const routes = [
  { path: '/login', component: Login },
  { path: '/items', component: ItemList },
  { path: '/items/create', component: ItemCreate },
  { path: '/items/:id', component: ItemDetail },
  { path: '/my-feedback', component: MyFeedback }
]
```

---

## 六、页面核心流程说明

### 1. 发起事项
1. 用户进入「新建事项」
2. 填写标题、说明、截止时间
3. 提交后状态 = collecting

---

### 2. 填写反馈
1. 用户进入事项详情页
2. 填写反馈内容
3. 提交后可再次编辑或撤销

---

### 3. 汇总与关闭
1. 发起人查看反馈列表
2. 导出 Excel / PDF（v2）
3. 将事项状态设为 closed

---

## 七、Mock 数据规范（前端先行）

```js
export const mockItem = {
  id: '1',
  title: '项目进度反馈',
  description: '请填写目前完成情况',
  status: 'collecting'
}
```

---

## 八、技术选型建议（与你当前环境匹配）

### 后端
- Python 3.x（Anaconda）
- FastAPI
- SQLite（本地 / 轻量）

### 前端
- Vue 3 或 React
- Axios
- Mock.js

---

## 九、版本规划

### v1（当前）
- 用户 / 事项 / 反馈
- 手动进入系统

### v2
- 文件上传
- 导出 Excel / PDF
- 提醒（邮件 / 企业微信）

---

## 十、交付状态说明

- 本文档：✅ 完整需求基线
- API：✅ Skeleton 可直接编码
- 前端：✅ 可 Mock 独立开发

> **此版本即为“最终合并版需求文档”，不再拆分、不再反复确认，可直接进入开发阶段。**

