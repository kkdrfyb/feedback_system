# 事项反馈系统（最终合并版需求文档）

> 版本：v1.1（与当前实现对齐）  
> 目标：一个**内部使用、轻量、可扩展**的事项收集 / 反馈 / 跟踪工具，支持后续文件、导出和提醒能力。

---

## 一、总体目标与设计原则

### 1. 建设目标
- 用于**发起事项 → 多人反馈 → 汇总与跟踪**
- 替代微信群 / Excel / 邮件反复统计
- 支持最小可用（MVP），后续平滑扩展

### 2. 设计原则
- 前后端分离
- API 简单、可 Mock
- 先流程正确，再优化体验
- 权限最简（不做复杂 RBAC）
- 基于 Token 的基础鉴权

---

## 二、核心业务对象（数据模型）

### 1. 用户（User）
```json
{
  "id": "int",
  "name": "string",
  "username": "string",
  "role": "admin | creator | feedbacker",
  "group": "string|null",
  "created_at": "datetime"
}
```

---

### 2. 事项（Item）
```json
{
  "id": "int",
  "title": "string",
  "description": "string",
  "creator_id": "int",
  "status": "ongoing | finished",
  "must_feedback": "bool",
  "deadline": "datetime",
  "attachments": "json|null",
  "created_at": "datetime"
}
```

---

### 3. 事项分配（ItemUser）
```json
{
  "id": "int",
  "item_id": "int",
  "user_id": "int",
  "feedback_status": "pending | done | completed",
  "last_feedback_time": "datetime|null"
}
```

---

### 4. 反馈（Feedback）
```json
{
  "id": "int",
  "item_user_id": "int",
  "content": "string",
  "created_at": "datetime",
  "updated_at": "datetime"
}
```

---

### 5. 附件（Attachment，v2 启用）
```json
{
  "id": "uuid",
  "related_type": "item | feedback",
  "related_id": "uuid",
  "filename": "string",
  "filepath": "string",
  "uploaded_at": "datetime"
}
```

---

## 三、后端 API 设计（Skeleton + 可直接实现）

### 0. 认证接口

| 方法 | 路径 | 描述 |
|---|---|---|
| POST | /api/login | 登录获取 Token |

---

### 1. 用户接口

| 方法 | 路径 | 描述 |
|---|---|---|
| GET | /api/users | 用户列表 |
| POST | /api/users | 新建用户 |
| DELETE | /api/users/{id} | 删除用户 |
| GET | /api/users/export | 导出用户 CSV |

---

### 2. 事项接口

| 方法 | 路径 | 描述 |
|---|---|---|
| GET | /api/items | 获取事项列表 |
| POST | /api/items | 创建事项 |
| GET | /api/items/{id} | 事项详情 |
| PUT | /api/items/{id} | 更新事项 |
| DELETE | /api/items/{id} | 删除事项 |
| GET | /api/items/stats/summary | 统计概览 |

---

### 3. 反馈接口

| 方法 | 路径 | 描述 |
|---|---|---|
| GET | /api/items/{id}/feedbacks | 事项反馈列表 |
| POST | /api/items/{id}/feedbacks | 提交反馈 |
| PUT | /api/feedbacks/{id} | 修改/撤销反馈 |
| GET | /api/todos | 获取待办事项 |

---

### 4. 分组与日志接口

| 方法 | 路径 | 描述 |
|---|---|---|
| GET | /api/groups | 分组列表 |
| POST | /api/groups | 创建分组 |
| PUT | /api/groups/{id} | 更新分组 |
| DELETE | /api/groups/{id} | 删除分组 |
| POST | /api/groups/import | 分组导入 |
| POST | /api/groups/sync_org | 同步组织分组 |
| GET | /api/operation_logs | 操作日志 |

---

### 5. 文件接口（v2）

| 方法 | 路径 | 描述 |
|---|---|---|
| POST | /api/upload | 上传文件 |
| GET | /api/export/item/{id} | 导出事项数据 |

---

## 四、前端页面清单

### 1. 页面列表

| 页面 | 路径 | 描述 |
|---|---|---|
| 登录页 | /login | 登录获取 Token |
| 工作台 | / | 事项列表与搜索 |
| 事项列表 | /items | 事项列表（别名） |
| 新建事项 | /item/create | 创建事项 |
| 事项详情 | /item/detail/:id | 填反馈 / 看汇总 |
| 待办反馈 | /todo | 当前用户待办 |
| 数据统计 | /stats | 统计概览 |
| 分组管理 | /groups | 分组与用户 |
| 操作日志 | /logs | 系统操作日志 |
| 反馈页 | /feedback/:itemId | 提交反馈 |

---

## 五、前端路由表（Vue / React 通用）

```ts
const routes = [
  { path: '/login', component: Login },
  { path: '/', component: Dashboard },
  { path: '/items', component: Dashboard },
  { path: '/item/create', component: ItemCreate },
  { path: '/item/detail/:id', component: ItemDetail },
  { path: '/feedback/:itemId', component: Feedback },
  { path: '/todo', component: Todo },
  { path: '/stats', component: DataStats },
  { path: '/groups', component: GroupManagement },
  { path: '/logs', component: OperationLog }
]
```

---

## 六、页面核心流程说明

### 1. 发起事项
1. 用户进入「新建事项」
2. 填写标题、说明、截止时间
3. 提交后状态 = ongoing

---

### 2. 填写反馈
1. 用户进入事项详情页
2. 填写反馈内容
3. 提交后可再次编辑或撤销

---

### 3. 汇总与关闭
1. 发起人查看反馈列表
2. 导出 Excel / PDF（v2）
3. 将事项状态设为 finished

---

## 七、Mock 数据规范（前端先行）

```js
export const mockItem = {
  id: '1',
  title: '项目进度反馈',
  description: '请填写目前完成情况',
  status: 'collecting'
}
```

---

## 八、技术选型建议（与你当前环境匹配）

### 后端
- Python 3.x（Anaconda）
- FastAPI
- SQLite（本地 / 轻量）

### 前端
- Vue 3 或 React
- Axios
- Mock.js

---

## 九、版本规划

### v1（当前）
- 用户 / 事项 / 反馈
- 手动进入系统

### v2
- 文件上传
- 导出 Excel / PDF
- 提醒（邮件 / 企业微信）

---

## 十、交付状态说明

- 本文档：✅ 完整需求基线
- API：✅ Skeleton 可直接编码
- 前端：✅ 可 Mock 独立开发

> **此版本即为“最终合并版需求文档”，不再拆分、不再反复确认，可直接进入开发阶段。**
