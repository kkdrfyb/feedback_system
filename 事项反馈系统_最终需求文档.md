# 事项反馈系统需求基线与差异清单

> 版本：v1.3（按 2026-08-07 源码、配置与已确认产品决策）  
> 阅读版：`事项反馈系统_最终需求文档.html`  
> 文档目的：把**已经实现的事实**、**尚待确认的产品选择**和**需要修复的问题**分开，供需求确认使用。

## 1. 已确认的业务目标

系统用于内部「发起事项 → 指派人员 → 提交反馈 → 汇总跟踪」。核心目标是替代微信群、Excel 和邮件中的重复催收与人工统计；当前定位为局域网或内部部署的轻量系统。

## 2. 当前交付范围（源码已实现）

| 模块 | 当前行为 | 状态 |
| --- | --- | --- |
| 认证 | 用户名密码登录，JWT Bearer Token，60 分钟有效期 | 已实现 |
| 用户 | 管理员可查询、创建、删除、导出 CSV | 已实现 |
| 事项 | 创建、附件、参与人分配、筛选、分页、详情、更新、删除、CSV 导出 | 已实现 |
| 反馈 | 待办、按事项/分配记录提交、本人修改；全员完成后自动结束事项 | 已实现 |
| 分组 | 个人分组、组织分组同步、CSV 导入 | 已实现 |
| 统计 | 事项、反馈、完成率、近期事项、部门排名 | 已实现 |
| 表格协作 | 上传 `.xlsx`、网页编辑、行级编辑控制、导出 `.xlsx`；所有登录用户可查看、导出 | 已实现（正式纳入范围） |
| 日志 | 登录、创建事项、删除事项等操作留痕；管理员查看 | 部分实现 |
| 外部提醒 | 无邮件、企业微信或站内通知需求；现有控制台临期输出不属于产品功能 | 不纳入范围 |

## 3. 角色与权限需求（按后端实际行为）

| 操作 | admin | creator / feedbacker |
| --- | --- | --- |
| 查看事项/统计 | 全部 | 仅本人发起或参与 |
| 创建事项 | 可指定发起人 | 服务端强制以当前用户为发起人 |
| 更新、删除事项 | 任意事项 | 仅本人创建的事项 |
| 提交/修改反馈 | 可代提交 | 仅本人被分配记录 |
| 用户、日志、全量导出 | 可用 | 禁止 |
| 分组 | 查看、编辑、删除所有分组；个人分组在「用户分组」默认折叠 | 查看组织分组和本人个人分组；可创建和维护本人非组织分组，其他人的个人分组不展示 |
| 表格 | 编辑、删除所有表格 | 所有登录用户可查看、导出；是否能编辑由负责人列决定 |

已确认：所有登录用户可在前端创建事项和维护自己的个人分组；管理员可在折叠的「用户分组」中查看和管理全部个人分组。后端始终根据登录用户执行归属校验。

## 4. 数据对象

| 对象 | 关键字段/关系 |
| --- | --- |
| User | `username` 唯一、`name`、`role`、`group`、`password_hash` |
| Item | 标题、说明、截止时间、状态、发起人、JSON 附件列表 |
| ItemUser | 事项—参与人分配；唯一约束为 `(item_id, user_id)`；反馈状态与最近反馈时间 |
| Feedback | 一条分配记录至多一条反馈；可更新内容 |
| Group | 名称、说明、组织组标志、所有者，与用户多对多 |
| Spreadsheet / SpreadsheetRow | 表格元数据、列配置、可选负责人列及行数据 JSON |
| OperationLog | 操作用户、动作、目标、时间 |

## 5. 前端页面（当前路由）

| 页面 | 路径 |
| --- | --- |
| 登录 | `/login` |
| 工作台 / 事项列表 | `/`、`/items` |
| 新建事项 | `/item/create`、`/items/create` |
| 事项详情 | `/item/detail/:id`、`/items/:id` |
| 填写反馈、待办 | `/feedback/:itemId`、`/todo` |
| 统计、日志、分组 | `/stats`、`/logs`、`/groups` |
| 表格列表、编辑 | `/spreadsheets`、`/spreadsheet/:id` |

## 6. 已确认需求与待确认项

| 编号 | 原需求/预期 | 当前实际 | 需要确认的决定 |
| --- | --- | --- | --- |
| D1 | v2 才有文件上传和导出 | 事项附件、CSV 导出、Excel 表格导出均已存在；没有 PDF | 将这些能力纳入 v1，或明确拆分为 v1.x。 |
| C1 | 所有员工创建事项 | 已确认：所有登录用户可创建本人事项；前端已开放入口 | 已实现 |
| C2 | 个人分组可见性 | 已确认：创建者和管理员可见；其他用户不展示；管理员默认折叠到「用户分组」 | 已实现 |
| C3 | 在线协作表格 | 已确认：正式纳入范围；所有登录用户可查看、导出 | 已实现 |
| C4 | 外部提醒 | 已确认：不需要邮件、企业微信等提醒功能 | 不纳入范围 |
| D6 | 反馈“撤销” | 接口仅支持修改，无删除/撤销语义 | 需要补充撤销吗？撤销后事项是否重新变为进行中？ |
| D7 | 附件对象独立建模 | 当前附件只存 Item 的 JSON 字段，不支持反馈附件 | 是否需要反馈附件、下载权限、版本/删除策略？ |

## 7. 项目问题与建议优先级

| 优先级 | 问题 | 影响与建议 |
| --- | --- | --- |
| P1 | 事项附件以 `/uploads/*` 静态公开 | 获得或猜到 URL 的人可直接下载，绕过事项权限。改为受鉴权下载接口或至少采用不可公开的存储策略。 |
| P1 | 附件上传没有文件大小、类型/内容校验 | 可造成磁盘耗尽或上传不当文件。应设大小上限、白名单、病毒扫描（如有条件）和失败清理。 |
| P1 | `JWT_SECRET_KEY` 有固定开发默认值，初始化脚本生成 `admin/123` | 误部署到生产会造成可预测登录/令牌风险。生产启动应强制环境变量，演示账号必须显式启用并要求改密。 |
| P2 | 删除事项不删除磁盘附件；更新附件也无回收策略 | 长期会留下孤儿文件、浪费存储。应做引用计数或受控清理任务。 |
| P2 | 新建事项/分组时未显式校验所有参与人/用户 ID 均存在 | SQLite 外键默认未启用时可能留下无效关联或静默遗漏。请求应在写入前校验并返回 400。 |
| P2 | 依赖版本未锁定；前端产物单包约 1.25 MB（gzip 约 402 KB） | 部署不可复现、首屏可能偏慢。锁定后端依赖并按路由拆包。 |
| P2 | 测试覆盖仍偏少 | 已补充个人分组可见性与可指派用户目录测试；仍缺附件安全、迁移及前端交互测试。 |
| P3 | 操作日志覆盖有限 | 审计范围不足；可在核心写操作补充日志。外部提醒已明确不纳入范围。 |

## 8. 验证状态

- `pytest -q`：10 passed（本轮新增个人分组可见性与可指派用户目录回归）。
- `frontend/npm run build`：构建成功；Vite 提示主包超过 500 KB。
- 本轮已更新前后端权限入口、API 回归测试和项目/需求文档。

## 9. 建议的下一轮确认顺序

1. 优先修复附件公开访问、上传校验、开发密钥和演示账号三个 P1 安全问题。
2. 补充附件安全、迁移和前端交互回归测试。
3. 确认 PDF 导出、反馈撤销和反馈附件是否进入下一个版本。
