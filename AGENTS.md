# AGENTS.md

## 项目目标
- 这是内部事项反馈系统（FastAPI + Vue + SQLite）。
- 优先保证权限边界、数据一致性、可回归测试，其次再做体验优化。

## 代码修改优先级
1. 安全与鉴权：禁止信任前端传入的 `role/user_id/owner_id` 作为授权依据。
2. 业务正确性：事项、分组、反馈操作必须做归属校验。
3. 稳定性：修改后必须跑 `pytest -q`。
4. 可维护性：优先小步可验证改动，避免一次性大重写。

## 后端约束
- 所有受保护接口统一依赖 `get_current_user`。
- 授权判断基于服务端 `current_user`，不基于前端本地存储参数。
- 禁止导出密码哈希等敏感字段。
- 文件上传必须做文件名清洗并使用唯一命名，避免覆盖与路径穿越风险。

## 前端约束
- 前端仍可传筛选参数，但不得承担鉴权责任。
- 角色只用于 UI 展示与菜单控制，不作为最终安全边界。

## 回归要求
- 最低回归：`pytest -q`
- 涉及权限变更时，补充/更新 API 测试用例。
